# Os arquivos .glb dos bonés reais entram aqui.
# Referencie-os sempre via `${import.meta.env.BASE_URL}models/arquivo.glb`
# (nunca com caminho absoluto "/models/...") para funcionar sob o base path do GitHub Pages.
